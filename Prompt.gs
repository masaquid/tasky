function getPromptTemplate() {
  return `
以下のタスク一覧を読み取り、それぞれに「高・中・低」の優先順位をつけてください。
優先順位は緊急性と重要度のバランスで判断してください。
各タスクに対して一言コメントも添えてください。

重要：必ず提供されたタスクのみを分析してください。データがない場合や不明な場合は、架空のタスクを作成しないでください。

出力例:
1. タスク名（優先度：高） - コメント
2. タスク名（優先度：中） - コメント
...

タスクデータ:
  `.trim();
}
